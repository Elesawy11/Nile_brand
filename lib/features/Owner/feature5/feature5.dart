import 'package:flutter/material.dart';

class Feature5 extends StatelessWidget {
  const Feature5({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Feature 5',
        ),
      ),
    );
  }
}
