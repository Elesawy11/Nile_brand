import 'package:flutter/material.dart';

class Feature6 extends StatelessWidget {
  const Feature6({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Feature 6',
        ),
      ),
    );
  }
}
