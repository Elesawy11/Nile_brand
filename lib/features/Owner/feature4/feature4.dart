import 'package:flutter/material.dart';

class Feature4 extends StatelessWidget {
  const Feature4({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Feature 4',
        ),
      ),
    );
  }
}
